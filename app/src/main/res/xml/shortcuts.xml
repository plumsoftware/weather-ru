<?xml version="1.0" encoding="utf-8"?>
<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:shortcutId="compose"
        android:enabled="true"
        android:icon="@drawable/weather_logo"
        android:shortcutShortLabel="@string/shortcut1_short_label"
        android:shortcutLongLabel="@string/shortcut1_long_label"
        android:shortcutDisabledMessage="@string/shortcut1_disabled_message">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="ru.plumsoftware.weatherforecastru"
            android:targetClass="ru.plumsoftware.weatherforecastru.application.shortcut.widgetconfig.WidgetConfigActivity" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
        <capability-binding android:key="actions.intent.CREATE_MESSAGE" />
    </shortcut>
    <!-- Specify more shortcuts here. -->
</shortcuts>